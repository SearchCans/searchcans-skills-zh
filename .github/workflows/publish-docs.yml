name: 发布中文文档站

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "scripts/generate_docs.py"
      - "skills/**/SKILL.md"
      - "README.md"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: python scripts/generate_docs.py --check
      # A repository-scoped token is required only while GitHub creates the
      # initial Pages site record. Reuse the documented automation token so
      # the public workflow never relies on a token embedded in repository code.
      - uses: actions/configure-pages@v5
        with:
          enablement: true
          token: ${{ secrets.WIKI_SYNC_TOKEN }}
      - uses: actions/upload-pages-artifact@v4
        with:
          path: docs/site
      - id: deployment
        uses: actions/deploy-pages@v4
